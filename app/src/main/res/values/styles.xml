<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="KetoneNumberPickerTheme" parent="AppTheme">
        <item name="colorControlNormal">@color/ketone</item>
        <item name="android:textColorPrimary">@color/ketone</item>
    </style>

    <style name="GlucoseNumberPickerTheme" parent="AppTheme">
        <item name="colorControlNormal">@color/glucose</item>
        <item name="android:textColorPrimary">@color/glucose</item>
    </style>

    <style name="Toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
         <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>
</resources>
